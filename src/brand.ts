/**
 * Product name, in one place.
 *
 * "Ponticello" replaced the working title "Cello Hero", which promised points
 * and a scoreboard — the one thing this app deliberately refuses. Referenced
 * from here rather than typed into screens, so a further rename is this file
 * plus `app.json`.
 */
export const APP_NAME = 'Ponticello';

/** Shown under the name on the library screen. */
export const APP_TAGLINE = 'Practice, not points';
